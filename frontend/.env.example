# Base URL of the Express backend API.
# The backend mounts every route under `/api`, so include that suffix here.
# Endpoint definitions use bare resource paths (e.g. "/auth/login") on top of this.
#
# Local development default (backend runs on port 5000):
VITE_API_BASE_URL=http://localhost:5000/api
